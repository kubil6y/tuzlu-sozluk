import { Inter, Shadows_Into_Light } from "next/font/google";

export const fontSans = Inter({ subsets: ["latin"], variable: "--font-sans" });
export const titleFont = Shadows_Into_Light({
    subsets: ["latin"],
    weight: ["400"],
});
